<div class="main-content container">  <h1>All Users</h1>
    <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.first_name + " " + user.last_name, user %></li>
    <% end %>
    </ul>
</div>
